Go 1.25；HTTP 框架为标准库 `net/http`；QUIC 使用 `quic-go`；eBPF/XDP 使用 `cilium/ebpf` 并在 Linux 上编译内嵌 C 程序；SQLite 使用纯 Go 实现 `modernc.org/sqlite`；GeoIP 查询使用 `maxminddb-golang`；前端仪表盘基于 Vue3 + TypeScript + Vite，由 Go 直接 serve 静态文件。