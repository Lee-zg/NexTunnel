工作区初始化：`make install-deps`（依次在各子模块执行 go mod tidy 与 npm install）。常用流程：`make dev` 启动桌面开发、`make build` 构建桌面应用、`make package-desktop|package-cli|package-server` 生成发行包、`make test` 运行全部 Go 与前端测试、`make verify-dashboard|verify-tun|verify-p2p-tun|verify-edge|verify-ebpf-linux` 触发端到端验证脚本。