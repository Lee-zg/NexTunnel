- 每个可发布组件使用独立 `go.mod`，并通过根 `go.work` 聚合，禁止跨模块直接引用相对路径。
- 跨组件公共能力收敛至 `pkg/`，其他模块仅通过 Go module 引用该包，避免重复实现。
- 构建与测试入口统一走根 `Makefile` 目标，子模块不再对外暴露独立的构建命令。
- 前端资源按组件拆分到各自 `frontend/` 或 `web/` 目录，由顶层 Makefile 在打包阶段分别构建。